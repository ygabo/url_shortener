<%= form_for(@link) do |f| %>
  <% if @link.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link.errors.count, "error") %> prohibited this link from being saved:</h2>

      <ul>
      <% @link.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link_in %><br />
    <%= f.text_field :link_in %>
  </div>
  <div class="field">
    <%= f.label :link_out %><br />
    <%= f.text_field :link_out %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
